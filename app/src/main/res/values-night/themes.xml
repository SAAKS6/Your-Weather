<resources>
    <!-- Application theme. -->
    <style name="Theme.WeatherApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- Dark Mode -->
        <item name="colorPrimary">@color/dark_blue_darkMode</item>
        <item name="colorSecondary">@color/white_darkMode</item>
        <item name="color">@color/white_darkMode</item>
        <item name="colorAccent">@color/light_blue_darkMode</item>
        <item name="srcCompat">@drawable/landing_bg_dark</item>
        <item name="textFillColor">@color/white_darkMode</item>
    </style>
</resources>